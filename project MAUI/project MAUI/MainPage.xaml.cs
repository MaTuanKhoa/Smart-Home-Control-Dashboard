using Microsoft.Maui.Controls;

namespace project_MAUI
{
    public partial class MainPage : ContentPage
    {
        public MainPage()
        {
            InitializeComponent();
            // UI only – no runtime logic required (as per assignment).
        }
    }
}